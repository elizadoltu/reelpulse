# ── GCP Project ─────────────────────────────────────────────────────────────
# Found at: console.cloud.google.com → select project → Project ID in header
GCP_PROJECT_ID=your-project-id

# ── BigQuery ─────────────────────────────────────────────────────────────────
# Found at: console.cloud.google.com → BigQuery → your dataset
BIGQUERY_DATASET=reelpulse
BIGQUERY_TABLE=movie_views

# ── Local dev ────────────────────────────────────────────────────────────────
# Use Functions Framework to run locally:
#   npx @google-cloud/functions-framework --target=analyticsProcessor
# Set GOOGLE_APPLICATION_CREDENTIALS to a service account key JSON for local auth:
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/key.json
